name: Close Stale PRs

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: lendingloop/github-actions/.github/workflows/stale-prs.yml@main
